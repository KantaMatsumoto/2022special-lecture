[tox]
envlist = py39
skipsdist = true

[testenv]
deps = coverage

commands = 
    coverage run -m unittest
    coverage xml

[coverage:run]
relative_files = true
source = special-lecture/
branch = true